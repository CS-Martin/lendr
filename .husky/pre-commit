echo '🚀 Running pre-push checks...'

set -e

echo '🧹 Linting all packages (turbo lint)...'
npm run lint || exit 1

echo '🧹 Building all packages (turbo build)...'
npm run build || exit 1

echo '✅ Pre-push checks passed!'
exit 0